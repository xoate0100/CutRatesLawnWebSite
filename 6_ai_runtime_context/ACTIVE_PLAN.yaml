plan_id: ghl-lead-workflows
component: web
goal: Finish Go High Level workflows and production wiring for website lead forms
status: active
auto_commit: false
commit_frequency: checkpoint_based
related:
  - docs/integrations/GOHIGHLEVEL.md
  - 6_ai_runtime_context/OUTSTANDING_TASKS.yaml
  - proposals/DEC-GHL-LEADCONNECTOR.yaml
  - lib/ghl.ts
  - app/api/lead/route.ts

# Canonical backlog lives in OUTSTANDING_TASKS.yaml — keep these in sync when status changes.
tasks:
  - id: GHL-OPS-001
    name: Add GHL_* secrets to Vercel production env
    status: pending
    outputs:
      - Vercel env for v0-cut-rates-lawn-main-page

  - id: GHL-WF-001
    name: Website lead nurture workflow (tag website-lead)
    status: pending
    outputs:
      - GHL workflow triggered by website-lead
      - Opportunity in New Leads / Fresh Lead

  - id: GHL-WF-002
    name: Source-specific branching (contact vs quote vs newsletter)
    status: pending
    outputs:
      - Differentiated GHL paths by source:* tags

  - id: GHL-WF-003
    name: Map service tags / custom fields into staff-visible CRM data
    status: pending
    outputs:
      - Service + message custom fields populated from site leads

  - id: GHL-TEST-001
    name: End-to-end form → GHL smoke test (local + production)
    status: pending
    outputs:
      - Verified tagged contact + workflow fire

  - id: GHL-DEC-001
    name: Accept or revise DEC-GHL-LEADCONNECTOR proposal
    status: pending
    outputs:
      - Decision registry entry or cancelled proposal note

  - id: GHL-OPS-002
    name: Optional — grant Locations View on PIT
    status: pending
    outputs:
      - locations.get succeeds with current PIT
